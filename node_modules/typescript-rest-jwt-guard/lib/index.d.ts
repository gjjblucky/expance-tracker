/**
 * JWT-checking decorator to be used with typescript-rest as annotation.
 * Used when JWT is passed with cookies.
 */
export declare function JwtCookieGuard(tokenName: string): (_target: Object, _key: string | symbol, descriptor: PropertyDescriptor) => void;
/**
 * JWT-checking decorator to be used with typescript-rest as annotation.
 * Used when JWT is passed with authorization header.
 */
export declare function JwtHeaderGuard(tokenName: string): (_target: Object, _key: string | symbol, descriptor: PropertyDescriptor) => void;
/**
 * JWT-checking decorator to be used with typescript-rest as annotation.
 * Used when JWT is passed with request post body data.
 */
export declare function JwtBodyGuard(tokenKey: string): (_target: Object, _key: string | symbol, descriptor: PropertyDescriptor) => void;
//# sourceMappingURL=index.d.ts.map